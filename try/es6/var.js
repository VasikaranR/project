var hello={
   name:"vasi",
   age:21,
   roll:55
   
}
hello.name="alpha"
console.log(hello.name +" "+hello.age+" "+hello.roll);

// var -global scope 
// hoisting (declaration to the top & assignment at the same place )
// work where ever you define